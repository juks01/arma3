
						["--enemyReArm loaded"] remoteExec ["systemChat", [0, -2] select isDedicated];

/*

{ // Modify rig
	if (side _x == opfor) then {
		removeAllWeapons _x;
		removeAllItems _x;
		removeAllAssignedItems _x;
		removeUniform _x;
		removeVest _x;
		removeBackpack _x;
		removeHeadgear _x;
		removeGoggles _x;
	}

	// add wanted stuff

} forEach allUnits;

*/
